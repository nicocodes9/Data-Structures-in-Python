# wildcard import statement in python
from math import *
# this basically means to import everything from the module without specifying the specific name of the object.

print(pi)
print(cos(34))
print(tan(67))
